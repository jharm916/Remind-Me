//
//  ListEntry.swift
//  Remind Me
//
//  Created by Jeremy Harman on 7/5/15.
//  Copyright (c) 2015 Jeremy Harman. All rights reserved.
//

import Foundation
import UIKit

class ListEntry: NSObject {
    /*
    var hour = 0
    var minutes = 0
    var message: NSString = ""
    var isAM = false
    
    init(var hour: Int, var minutes: Int, var message: NSString, var isAM: Bool) {
        self.hour = hour
        self.minutes = minutes
        self.message = message
        self.isAM = isAM
    }
    
    func GetPaddedMinutes() -> String {
        if (self.minutes < 10) {
            return "0" + toString(self.minutes)
        }
        else {
            return toString(self.minutes);
        }
    }
    
    func GetTimePeriod() -> String {
        if (self.isAM) {
            return "AM"
        }
        else {
            return "PM"
        }
    }
    */
}